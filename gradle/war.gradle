apply plugin: "war"

bootWar {
    mainClassName = "com.icthh.xm.tmf.ms.qualification.QualificationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
